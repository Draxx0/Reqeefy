import { Settings, TicketSlash, Bell, Shuffle, LogOut } from 'lucide-react';
export const Icons = {
  ticket: TicketSlash,
  distribution: Shuffle,
  settings: Settings,
  logout: LogOut,
  notifications: Bell,
};
