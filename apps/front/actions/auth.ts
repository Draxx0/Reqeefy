'use server';
const login = () => {
  return 'login';
};

const register = () => {
  return 'register';
};

const logout = () => {
  return 'logout';
};

const AUTH = {
  login,
  register,
  logout,
};

export default AUTH;
